<Window x:Class="Курсовой.View.SignInWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:Курсовой.ViewModel"
        xmlns:local="clr-namespace:Курсовой.View"
        mc:Ignorable="d"
        Title="CD SHELL" Height="550" Width="400" WindowStartupLocation="CenterScreen"  Icon="pack://application:,,,/Resources/Logo.png" Style="{StaticResource WindowStyle}">
    <Window.DataContext>
        <vm:SignInWindowViewModel/>
    </Window.DataContext>
    <Frame Name="Frame" Grid.Column="1" Grid.Row="1" NavigationUIVisibility="Hidden" Source="{Binding MainPage, Mode=OneWay}">
    </Frame>
</Window>
